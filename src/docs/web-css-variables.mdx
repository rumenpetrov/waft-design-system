import { Meta, Source } from '@storybook/blocks';
import { renderTokens, isGlobalToken, transformTokenNameToWebTokenName } from '../design-tokens/index.ts';

<Meta title="Web/CSS variables" />

# Design tokens for web (CSS variables)
For web, all **design tokens** are converted to [CSS custom properties](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties). Their names are transformed to lowercase and 2 dashes are added at the beginning in order to comply with the web standards.

<table style={{ width: '100%' }}>
  <thead align="left">
    <tr>
      <th>Name</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    {renderTokens((tokenName, tokenValue) => (
      <tr key={tokenName}>
        <td>{transformTokenNameToWebTokenName(tokenName)}</td>
        <td>{tokenValue}</td>
      </tr>
    ), isGlobalToken)}
  </tbody>
</table>
